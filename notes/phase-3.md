# Phase 3：ZSet 与热点排行榜

当前策略是排行榜更新失败时让请求失败；
这会优先保证统计一致性，但牺牲一次跳转可用性；
后续需要比较事务、Lua 和异步 Stream 方案。

1. 查询排行榜时逐个检查 Hash 是否存在，删除失效 member
> 优点：查询时清理，实现简单，但排行榜查询变慢
2. URL 过期前显式删除 Zset member
> 显式删除，及时，但需要可靠的删除触发机制
3. 使用定时任务扫描并清理排行榜
> 定时扫描，查询稳定，但存在延迟和额外任务
