# Redis Shortlink Lab

一个使用 Node.js 手动实现的高并发短网址服务与 API 限流实验项目。

## 学习目标

通过一个逐步演进的应用，理解 Redis 不只是简单的 KV 缓存，而是一个提供多种数据结构、原子操作和异步消息能力的内存数据系统。

每个阶段都遵循三个原则：

1. 先自己实现最小功能，再阅读和比较不同方案。
2. 用 `redis-cli`、测试和故障实验观察 Redis 的真实行为。
3. 记录数据结构、并发语义、过期策略和适用边界，而不只记录 API 用法。

## 技术栈

- Node.js（优先使用原生 API，减少框架和封装带来的干扰）
- Redis
- Node.js Redis 客户端
- JavaScript 或 TypeScript（以仓库实际初始化选择为准）
- 后续按需要加入测试工具、Docker 和 SQLite/MySQL

## 分阶段路线

### Phase 0：项目骨架与实验环境

目标：建立可重复运行的本地开发环境。

- 创建最小 HTTP 服务和 Redis 连接模块
- 增加 `/health` 健康检查接口
- 使用环境变量配置服务端口和 Redis 地址；默认端口为 `3003`
- 准备 Redis CLI 操作和基础测试命令

验证重点：Redis 不可用时服务如何启动、报错和恢复连接。

基础服务验证：

```bash
curl -i http://localhost:3003/health
```

### Phase 1：String 与 TTL

目标：理解最基本的键值读写和过期机制。

实现接口：

- `POST /shorten`：提交长 URL，返回短码
- `GET /:code`：根据短码查找并跳转

Redis 实验：

- `SET`、`GET`
- `SET ... EX` 或 `SETEX`
- `TTL`、`EXPIRE`
- 观察过期键的惰性删除与定期删除行为

短码写入必须处理碰撞，不能无条件覆盖已有数据；可以先用 `SET NX` 实现占位和冲突重试。

### Phase 2：Hash 与原子计数

目标：使用 Hash 保存结构化元数据，并理解字段级更新。

建议字段：`target`、`created_at`、`click_count`。

- 使用 `HSET`、`HGETALL`、`HINCRBY`
- 访问短链接时原子增加点击次数
- 比较 Hash 与 JSON String 在读取和更新方面的差异

注意：Redis 的 TTL 作用于整个 key，不能只给 Hash 的某个字段设置过期时间。需要明确短链接数据过期后的清理策略。

### Phase 3：ZSet 与热点排行榜

目标：理解有序集合的排序和实时统计能力。

- 每次访问使用 `ZINCRBY` 更新热点分数
- 使用 `ZREVRANGE`（或当前 Redis 版本对应命令）查询 Top 10
- 用 `redis-cli` 观察成员、分数和排名变化

需要处理排行榜残留成员：URL Hash 过期不会自动删除 ZSet 中对应的成员。可以设计定期清理、查询时过滤或显式删除方案，并比较它们的代价。

### Phase 4：限流与 Lua 原子操作

目标：理解并发竞争，以及为什么需要在 Redis 内部执行原子逻辑。

按顺序实现：

1. Node.js 进程内计数器，认识单机方案的局限。
2. Redis `INCR` + `EXPIRE` 固定窗口限流。
3. Lua 脚本将计数、首次设置过期时间和阈值判断合并为原子操作。
4. 编写并发测试，比较脚本前后的行为。
5. 进一步实现滑动窗口或令牌桶，理解固定窗口边界突发问题。

示例规则：同一 IP 每 1 秒最多请求 5 次。

同时记录：脚本的 `KEYS`/`ARGV` 设计、脚本执行时间，以及 Redis 单线程执行模型对长脚本的影响。

### Phase 5：Stream 与异步访问日志

目标：理解 Redis 作为轻量级消息系统时的模型和边界。

- 访问短链接时用 `XADD` 写入访问事件
- 使用 `XGROUP`、`XREADGROUP` 创建消费者组并读取消息
- 正确处理 `XACK`、Pending Entries 和消费者重启
- 第一版写入本地文件或 SQLite，之后再接入 MySQL

需要测试消费者宕机、重复消费、积压和消息确认失败等情况。

## 必做验证

- 短码重复与碰撞重试
- URL 过期后的读取行为
- 并发点击计数是否准确
- 排行榜数据与点击数是否一致
- 限流边界和窗口切换行为
- Redis 重启、连接中断和消费者重启
- 观察 `MONITOR`、`INFO`、`SCAN`、`MEMORY USAGE` 的输出

## 后续生产化主题

完成上述单机实验后，再研究 Redis 持久化（RDB/AOF）、内存淘汰策略、缓存穿透/击穿/雪崩、主从复制、Sentinel、Cluster、热点 key 和高可用部署。

这些主题属于后续扩展，不应在最初阶段引入，以免掩盖数据结构和原子操作本身。

## 学习记录建议

每个 Phase 完成后，提交一次小版本，并在 README 或 `notes/` 中记录：

- 使用了哪些 Redis 命令和数据结构
- 请求路径和 key 的设计
- 哪些操作是原子的，哪些不是
- 失败场景和观察到的结果
- 当前方案的性能与一致性取舍
